<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #28a745; color: white; padding: 20px; border-radius: 5px; }
        .content { padding: 20px; border: 1px solid #ddd; margin-top: 20px; }
        .info { background: #f8f9fa; padding: 15px; border-left: 4px solid #007bff; margin: 10px 0; }
        .message-box { background: #fff; border: 1px solid #ddd; padding: 15px; margin: 15px 0; border-radius: 3px; }
        .button { display: inline-block; background: #007bff; color: white; padding: 10px 20px; text-decoration: none; border-radius: 3px; margin-top: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>🔔 New Contact Form Submission</h2>
        </div>
        <div class="content">
            <div class="info">
                <p><strong>From:</strong> <%= clientName %></p>
                <p><strong>Email:</strong> <%= clientEmail %></p>
                <p><strong>Subject:</strong> <%= subject %></p>
            </div>
            
            <h3>Message:</h3>
            <div class="message-box">
                <%= message %>
            </div>
            
            <a href="<%= dashboardUrl %>" class="button">View in Dashboard</a>
        </div>
    </div>
</body>
</html>